name: example
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0-113.1.beta

dependencies:
  file_picker: ^12.0.0
  flutter:
    sdk: flutter
  wa_me:
    path: ../

  material_ui: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
